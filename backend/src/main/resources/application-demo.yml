spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/arknights_db?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  sql:
    init:
      mode: always
      encoding: UTF-8
      schema-locations: file:./sql/ability_schema.sql

server:
  port: 8080
